<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/summary_page_item_bg"
    tools:context=".ui.patients.custom.GroupFragment">

    <!-- TODO: Update blank fragment layout -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@color/summary_page_item_bg"
        android:backgroundTint="@color/summary_page_item_bg"
        android:elevation="15dp"
        android:orientation="vertical"
        android:paddingVertical="@dimen/fab_margin"
        app:cardBackgroundColor="@color/summary_page_item_bg"
        app:cardCornerRadius="10dp"
        app:strokeColor="@color/summary_page_item_border"
        app:strokeWidth="1dp">

        <LinearLayout
            android:id="@+id/ln_parent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="10dp">

            <TextView
                android:id="@+id/tv_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="6dp"
                android:text="Clinical History"
                android:textColor="@color/summary_page_title_text"
                android:textSize="14sp"
                android:textStyle="bold" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</ScrollView>
